// Schema for chat history (for future use with a migration tool)
// Create a table in your MySQL database called `chat_history` with columns: id (INT, AUTO_INCREMENT), user_message (TEXT), bot_response (TEXT), language (VARCHAR), created_at (DATETIME, DEFAULT CURRENT_TIMESTAMP)

// Sample SQL for creating the table:
// CREATE TABLE chat_history (
//     id INT AUTO_INCREMENT PRIMARY KEY,
//     user_message TEXT,
//     bot_response TEXT,
//     language VARCHAR(10),
//     created_at DATETIME DEFAULT CURRENT_TIMESTAMP
// );
